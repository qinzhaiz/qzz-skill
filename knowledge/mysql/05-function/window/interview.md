# 面试题

## Q1：窗口函数和 GROUP BY 的区别？

**考点**：8.0 核心特性。

**回答**：GROUP BY 折叠行——多行压成一行，只保留聚合结果。窗口函数不折叠行——每行都保留，同时加上跨行计算的值（排名、累计、前后行）。需要分组但不折叠行 → 窗口函数。需要聚合统计 → GROUP BY。

## Q2：PARTITION BY 和 GROUP BY 在窗口函数里的区别？

**考点**：术语区分。

**回答**：PARTITION BY 是窗口函数的"分组"——决定了窗口的范围，但不合并行。GROUP BY 是查询的"分组"——合并行，每组只留一行。
