# 练习

## 基础练习

1. 开启事务，插入一条数据，ROLLBACK，确认数据没有被保存。

2. 开启事务，插入一条数据，COMMIT，确认数据被保存。

3. 用 Savepoint 模拟"先扣钱再发现转错人"的场景。

## 进阶练习

1. 两个终端同时开启事务，A 修改一行不提交，B 查询同一行——B 能看到 A 的修改吗？

## 答案

1-3 无标准答案。

4. 在 RR 级别下，B 看不到 A 未提交的修改——这就是MVCC 在起作用。如果 B 用 `FOR UPDATE`（当前读），则会阻塞等待 A 释放锁。
