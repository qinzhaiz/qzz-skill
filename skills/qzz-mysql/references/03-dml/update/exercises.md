# 练习

## 基础练习

1. 把 user 表里 id=1 的用户年龄改成 30。

2. 把 city='北京' 的用户状态改成 1。

3. **先在测试库里执行一次不带 WHERE 的 UPDATE，然后跑 SELECT 看看发生了什么。**

## 进阶练习

1. 用 UPDATE 配合表达式，把所有商品价格下调 10%。

2. 把 user 表里 2024 年之前注册的用户标记为 status=2，并设置 archived_at 为当前时间。用 LIMIT 分批执行。

## 答案

1-3 无标准答案。第 3 题的教训：整张表全被改了。
